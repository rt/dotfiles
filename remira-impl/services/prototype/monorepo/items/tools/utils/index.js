import loggerRef from './log';
import formatterRef from './format';
import runnerRef from './run';
import cpRef from './cp';
import fsRef from './fs';
import gitRef from './git';
import networkRef from './network';

export const logger = loggerRef;
export const formatter = formatterRef;
export const run = runnerRef;
export const cp = cpRef;
export const fs = fsRef;
export const git = gitRef;
export const network = networkRef;
