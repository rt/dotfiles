-- Rename columns only, not table.

-- Old names (as view) accessed by V12.1: xout(room_type_id, xdate, id)
-- New names (as view) accessed by V12.2: xout(room_type_id, blackout_date, blackout_id)
-- Shadow Table: xout_shadow(room_type_id, blackout_date, blackout_id)

